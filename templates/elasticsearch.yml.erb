# Managed by Puppet, DO NOT TOUCH!

transport:
        tcp:
                port: <%= esTCPPortRange %>

http:
        port: <%= esHTTPPortRange %>

cluster:
        name: "<%= clustername %>"

path:
        logs: <%= esLogPath %>
        data: <%= esDataPath %>

boostrap:
        mlockall: true
